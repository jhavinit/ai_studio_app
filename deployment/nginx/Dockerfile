# nginx/Dockerfile
FROM nginx:stable-alpine

# Copy custom config
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Copy built frontend (React, Vue, etc.)
COPY dist /usr/share/nginx/html

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
